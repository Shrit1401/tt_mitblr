/** API source stays separate from the frontend-only prototype build. */
export default {
  devIndicators: false,
  transpilePackages: ['@domino/engine', '@domino/contracts', '@domino/db', '@domino/data'],
};
